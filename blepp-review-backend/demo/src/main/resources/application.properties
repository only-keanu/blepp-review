spring.application.name=demo
spring.profiles.active=local

# Database (override via environment variables)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/blepp-review}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Dwkrp987}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# JWT (base64-encoded 256-bit secret; override in env)
app.jwt.secret=${APP_JWT_SECRET:bg95FrOZp6xGLJSgzideW+s31fFBsK1j/K1xu/wkXoo=}
app.jwt.access-expiration-minutes=${APP_JWT_ACCESS_EXPIRATION_MINUTES:30}
app.jwt.refresh-expiration-minutes=${APP_JWT_REFRESH_EXPIRATION_MINUTES:43200}
