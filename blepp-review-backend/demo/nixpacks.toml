[phases.setup]
nixPkgs = ["jdk21", "gradle"]

[phases.build]
cmds = ["bash ./gradlew bootJar -x test"]

[start]
cmd = "java -Dserver.port=${PORT:-8080} -Dspring.profiles.active=${SPRING_PROFILES_ACTIVE:-prod} -jar build/libs/demo-0.0.1-SNAPSHOT.jar"
